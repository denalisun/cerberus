#pragma once
#include "Globals.hpp"
#include "SDK.hpp"

using namespace SDK;
using namespace Globals;

namespace Player
{
	void SpawnPawn(FVector Location, FRotator Rotation);
	void ShowSkin();
	void DeployFromBus();
}